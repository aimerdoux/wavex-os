/** Auto-injected by esbuild into every JSX file so the classic
 *  `React.createElement(...)` transform resolves React without a manual
 *  `import React from "react"` line in each .tsx. The named import keeps
 *  the bundle small (no namespace wrapper) and works with the host's
 *  bare-specifier rewriter, which already resolves "react". */
export * as React from "react";
